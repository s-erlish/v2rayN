namespace ServiceLib.Models.Configs;

// Метаданные System.Text.Json для Config и всего, что в нём лежит, собранные при компиляции.
// Разбор конфига — самое первое, что делает приложение при запуске, ещё до окна. Отражением первый
// разбор стоил около 110 мс (замер сборки ReadyToRun): код доступа к свойствам STJ порождает на месте,
// и заранее его не скомпилировать. По готовым метаданным — единицы миллисекунд. Подключается в
// JsonUtils первым звеном цепочки: типы, которых здесь нет, по-прежнему идут через отражение.
[JsonSerializable(typeof(Config))]
internal partial class ConfigJsonContext : JsonSerializerContext;
