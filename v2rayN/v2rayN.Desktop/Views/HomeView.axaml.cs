namespace v2rayN.Desktop.Views;

/// <summary>
/// Главная — двухпанельная заглушка (§1.4). Реальный контент (мета-бар, список
/// серверов, connect-щит, стата-строка) подставляют агенты фазы 1.
/// </summary>
public partial class HomeView : UserControl
{
    public HomeView()
    {
        InitializeComponent();
    }
}
